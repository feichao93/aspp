.connection-panel {
  .client {
    display flex
    align-items center

    > svg {
      flex 0 0 auto
    }

    .username {
      flex 0 0 50px
      line-height 32px
      margin-left 8px
    }

    .status {
      word-break break-all
    }
  }
}
