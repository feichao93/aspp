.diff-panel {
  .diff-non-ideal-state {
    height 50%
  }

  .diff-distribution {
    .row {
      margin-top 8px
      > header {
        display flex
        align-items center
      }
    }
  }
}
